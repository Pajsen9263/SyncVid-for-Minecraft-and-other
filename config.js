const config = {
    // App settings
    port: 3000,
    requireAuth: false, 
    allowGuest: false, 
    defaultUsername: "admin",
    defaultPassword: "password",

  };
  
module.exports = config;
  